---
import PublicLayout from "@/layouts/PublicLayout.astro";
import { ArticleContent } from "@/components/articles/ArticleContent";

const { id } = Astro.params;
---

<PublicLayout>
    {id && <ArticleContent id={id} client:load />}
</PublicLayout>
