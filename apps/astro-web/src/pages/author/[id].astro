---
import PublicLayout from "@/layouts/PublicLayout.astro";
import { AuthorProfile } from "@/components/author/AuthorProfile";

const { id } = Astro.params;
---

<PublicLayout>
    {id && <AuthorProfile id={id} client:load />}
</PublicLayout>
